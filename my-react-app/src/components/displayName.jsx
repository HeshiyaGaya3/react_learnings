import React from "react";
import Form from "./form"; 

export default function DisplayName({ name }) {

    return (
        <div>
                <p>{name}</p>
                <Form/> 
        </div>
        
        
            
    
        
    );
}